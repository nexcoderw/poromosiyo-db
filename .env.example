# Poromosiyo local development database.
# This is a placeholder only. Do not commit real credentials.
DATABASE_URL="mysql://poromosiyo_user:change-me@127.0.0.1:3306/poromosiyo"

# Runtime consumers connect eagerly by default.
# Unit tests skip the initial database connection unless explicitly enabled.
DATABASE_CONNECT_ON_INIT=true
